export function detectCurrentSection(){const links=document.querySelectorAll(".nav__link");const observer=new IntersectionObserver(function(entries){entries.forEach(function(entry){if(entry.isIntersecting){const sectionId=entry.target.getAttribute("id");let link;if(sectionId!=="home"){link=document.querySelector(`.nav__link[href="#${sectionId}"]`)}
for(let i=0;i<links.length;i++){links[i].classList.remove("nav__link-active")}
if(sectionId!=="home"){link?.classList.add("nav__link-active")}}})},{threshold:0.5});const sections=document.querySelectorAll(".section");sections.forEach(function(section){observer.observe(section)});const sectionHome=document.getElementById("home");observer.observe(sectionHome)}